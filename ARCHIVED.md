# This branch is frozen

`V2-Improvements` is a read-only archive as of 2026-09-24. It never merges to `main`.

- Current code lives on **`main`** (GitHub default) and **`V4-Improvements`** (the working branch;
  Railway staging deploys it).
- Start from `CLAUDE.md` and `docs/REPO-MAP.md` on either of those branches.

What this branch keeps that exists nowhere else:
- `docs/knowledge/*`, `docs/options/lifecycle-stage-options.md`, the idea-ranking warm-starts plan,
  and `n8n/pathfinder-daily-audience-dnc.json` (commit `1be55b0`).
- `docs/context-workbench-attempt-project-brief.md` — a Sep 15 failure review of the Context Workbench.
- `docs/specs/waypoint-learning-loop.md` — the Aug 26 "Building a loop that can learn" build spec.
